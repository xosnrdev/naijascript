make counter get 0
make limit get 5

jasi (counter small pass limit) start
    make counter get counter add 1
    shout (counter)
end

if to say (counter na limit) start
    shout (999)
end if not so start
    shout (0)
end
