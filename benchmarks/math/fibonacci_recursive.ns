# Fibonacci recursive - NaijaScript
# Tests function call overhead and recursion performance

do fib(n) start
    if to say (n small pass 2) start
        return n
    end
    if not so start
        return fib(n minus 1) add fib(n minus 2)
    end
end

# Test with n=30 (reasonable for tree-walk interpreter)
make result get fib(30)
shout(result)