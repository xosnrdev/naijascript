make i get 1

jasi (i small pass 101) start
    if to say (i mod 3 na 0) start
        if to say (i mod 5 na 0) start
            shout("FizzBuzz")
        end
        if not so start
            shout("Fizz")
        end
    end
    if not so start
        if to say (i mod 5 na 0) start
            shout("Buzz")
        end
        if not so start
            shout(i)
        end
    end
    i get i add 1
end